# uDOS-plugin-index Getting Started

1. Start with the manifest contracts in `contracts/`.
2. Review `docs/activation.md`.
3. Align validation in `schemas/`.
4. Run `scripts/run-plugin-index-checks.sh`.
5. Add examples only when they teach a stable contract.
6. Keep tests focused on schema and manifest integrity.
